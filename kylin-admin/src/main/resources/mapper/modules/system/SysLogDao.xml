<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kylin.modules.system.dao.SysLogDao">

    <select id="queryObject" resultType="com.kylin.modules.system.entity.SysLogEntity">
		select * from sys_log where id = #{value}
	</select>

    <select id="queryList" resultType="com.kylin.modules.system.entity.SysLogEntity">
        select
        <choose>
            <when test="pageSize != null">
                top ${pageSize} TEMP.* FROM (
                SELECT ROW_NUMBER() OVER (order by create_date desc) AS RowNumber,* FROM dbo.sys_log
                ) AS TEMP
            </when>
            <otherwise>
                * from sys_log
            </otherwise>
        </choose>

        <where>
            <if test="key != null and key.trim() != ''">
                username like '%'+#{key}+'%' or operation like '%'+#{key}+'%'
            </if>
            <if test="pageNum != null and pageSize != null">
                and RowNumber > ${pageNum}*(${pageSize}-1)
            </if>
        </where>

    </select>

    <select id="queryTotal" resultType="int">
        select count(*) from sys_log
        <where>
            <if test="key != null and key.trim() != ''">
                username like '%'+#{key}+'%' or operation like '%'+#{key}+'%'
            </if>
        </where>
    </select>

    <insert id="save" parameterType="com.kylin.modules.system.entity.SysLogEntity" useGeneratedKeys="true"
            keyProperty="id">
		insert into sys_log
		(
			username,
			operation,
			method,
			params,
			time,
			ip,
			create_date
		)
		values
		(
			#{username}, 
			#{operation}, 
			#{method}, 
			#{params}, 
			#{time},
			#{ip},
			#{createDate}
		)
	</insert>

    <delete id="delete">
		delete from sys_log where id = #{value}
	</delete>

    <delete id="deleteBatch">
        delete from sys_log where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>